# RNW regionIdent table — region code -> regionIdent (profile 1 / CCP)
# regionIdent = (profile<<10)|codeId ; all shipped RNW regions use profile 1 (folder CCP).
# Baked into osm2rnw (src/osm2rnw/src/main.rs REGION_IDENT).
#
# REBUILT 2026-09 with a trustworthy method. Every .tci clusterRef is
#   {u32 fileOffset = (aligned cluster offset & ~0x3FFF) | regionIdent,
#    u16 length, u16 fileId}                       <- field order CONFIRMED from DAPIAPP.OUT
# (u16LoadClusterIdListAndStoreInQ disasm @0x8deb20: struct+4 -> vSetClusterLength,
#  struct+6 -> vSetClusterFileId; older docs had these swapped, osm2rnw fixed accordingly).
# Decoding all 76 EUR-card shards and requiring fileId+inbounds to resolve to exactly ONE
# region's NAV%05u.DAT inventory gives an unambiguous ident -> owner-region vote:
#   0x401->DEU 94546/94546 · 0x409->IBE 72062 · 0x40a->FRM 3098 · 0x40b->ISV 9215 ·
#   0x40d->SCA 128129 · 0x42a->EEU 115744   (100 % agreement, single-owner refs only)
#
# WARNING about the previous table: it was derived from NAV_ROOT.DAT low-14-bit histograms
# and is demonstrably WRONG in at least two rows (FRM listed 0x403, MLC listed 0x40a; 0x40a
# empirically owns FRM files). Rows without ref-join evidence (BNL GRC TUR ACL MLC GBI CHS
# ELL INT EAD) were REMOVED — a guessed ident silently emits another region's clusters.
# Target those regions with an explicit --region-ident once proven (on-device open-capture).
#
# POL caveat: NO .tci shard on this card references POL's clusters (all 76 shards checked),
# and Krakow's own shard N6E2102.TCI references cluster files that are absent from the card.
# The runtime evidently locates Poland's road clusters another way (NAV_ROOT.DAT /
# NAV00001.DAT per-region container). 0x402 for POL is a legacy value, never confirmed.
region	regionIdent	codeId	profile	confidence
DEU	0x0401	1	1	confirmed(ref-join 94546)
POL	0x0402	2	1	unverified(no .tci refs on card; card loads POL via NAV_ROOT/NAV00001)
IBE	0x0409	9	1	confirmed(ref-join 72062)
FRM	0x040a	10	1	confirmed(ref-join 3098; old 0x403 was wrong)
ISV	0x040b	11	1	confirmed(ref-join 9215)
SCA	0x040d	13	1	confirmed(ref-join 128129)
EEU	0x042a	42	1	confirmed(ref-join 115744)
